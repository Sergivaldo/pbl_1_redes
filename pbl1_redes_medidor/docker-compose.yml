version: "2"
services:
  server:
    image: sergivaldo/pbl1_redes:pbl1_meter
    stdin_open: true
    tty: true
#    build:
#      context: .
#      dockerfile: Dockerfile
    restart: always
    volumes:
      - /app
